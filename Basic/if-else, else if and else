#include <stdio.h>

int main() {

    int x;
    
    printf("Type an integer:");
    scanf("%d", &x);
    if(x>10)
    {
        printf("You typed %d.\n",x);
        printf("%d is greater than 10.\n", x);
    }   
    else if(x<10)
    {
        printf("You typed %d.\n",x);
        printf("%d is less than \n", x);
    }
    else
    {
        printf("you typed %d.\n",x);
        printf("number you typed was %d.\n", x);
    }

    return 0;
}
